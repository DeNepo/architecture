{
  "title": "something",
  "description": "some test data for the 'DOMContentLoaded' tests (that don't exist yet)",
  "required": ["aNumber"],
  "additionalProperties": false,
  "properties": {
    "aNumber": {
      "type": "string",
      "description": "a number written out as a word"
    }
  }
}
